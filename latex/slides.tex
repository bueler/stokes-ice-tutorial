% Copyright 2021-2026  Ed Bueler

\documentclass[10pt,
               hyperref={colorlinks,citecolor=DeepPink4,linkcolor=black,urlcolor=blue},
               svgnames]{beamer}

\mode<presentation>{
  \usetheme{Madrid}
  \usecolortheme{beaver}
  \setbeamercovered{transparent}
  \setbeamerfont{frametitle}{size=\large}
}

\setbeamercolor*{block title}{bg=red!10}
\setbeamercolor*{block body}{bg=red!5}

\usepackage[english]{babel}
\usepackage[latin1]{inputenc}
\usepackage{times,stmaryrd}
\usepackage[T1]{fontenc}
% Or whatever. Note that the encoding and the font should match. If T1
% does not look nice, try deleting the line with the fontenc.

\usepackage{amssymb,pifont}
\usepackage{empheq}
\usepackage{xspace}
\usepackage{verbatim,fancyvrb}

\usepackage{tikz}
\usetikzlibrary{shapes,arrows.meta,decorations.markings,decorations.pathreplacing,fadings,positioning}

\usepackage{hyperref}

% If you wish to uncover everything in a step-wise fashion, uncomment
% the following command:
%\beamerdefaultoverlayspecification{<+->}

\newcommand{\ba}{\mathbf{a}}
\newcommand{\bb}{\mathbf{b}}
\newcommand{\bc}{\mathbf{c}}
\newcommand{\bbf}{\mathbf{f}}
\newcommand{\bg}{\mathbf{g}}
\newcommand{\bn}{\mathbf{n}}
\newcommand{\bq}{\mathbf{q}}
\newcommand{\br}{\mathbf{r}}
\newcommand{\bx}{\mathbf{x}}
\newcommand{\by}{\mathbf{y}}
\newcommand{\bv}{\mathbf{v}}
\newcommand{\bu}{\mathbf{u}}
\newcommand{\bw}{\mathbf{w}}

\newcommand{\bF}{\mathbf{F}}
\newcommand{\bG}{\mathbf{G}}
\newcommand{\bQ}{\mathbf{Q}}

\newcommand{\bzero}{\mathbf{0}}

\newcommand{\cK}{\mathcal{K}}

\newcommand{\grad}{\nabla}
\newcommand{\Div}{\nabla\cdot}
\newcommand{\minmod}{\operatorname{minmod}}

\newcommand{\CC}{\mathbb{C}}
\newcommand{\RR}{\mathbb{R}}

\newcommand{\ddt}[1]{\ensuremath{\frac{\partial #1}{\partial t}}}
\newcommand{\ddx}[1]{\ensuremath{\frac{\partial #1}{\partial x}}}
\newcommand{\Matlab}{\textsc{Matlab}\xspace}
\newcommand{\Octave}{\textsc{Octave}\xspace}
\newcommand{\eps}{\epsilon}

\newcommand{\ip}[2]{\left<#1,#2\right>}

\newcommand{\trefcolumn}[1]{\begin{bmatrix} \phantom{x} \\ #1 \\ \phantom{x} \end{bmatrix}}
\newcommand{\trefmatrixtwo}[2]{\left[\begin{array}{c|c|c} & & \\ #1 & \dots & #2 \\ & & \end{array}\right]}
\newcommand{\trefmatrixthree}[3]{\left[\begin{array}{c|c|c|c} & & & \\ #1 & #2 & \dots & #3 \\ & & & \end{array}\right]}
\newcommand{\trefmatrixgroups}[4]{\left[\begin{array}{c|c|c|c|c|c} & & & & & \\ #1 & \dots & #2 & #3 & \dots & #4 \\ & & & & & \end{array}\right]}

\newcommand{\blocktwo}[4]{\left[\begin{array}{c|c} #1 & #2 \\ \hline #3 & #4 \end{array}\right]}

\newcommand{\cmark}{\ding{51}}
\newcommand{\xmark}{\ding{55}}


\newcommand{\ds}{\displaystyle}

\newcommand{\sdoi}[1]{\,{\tiny \href{https://doi.org/#1}{doi:#1}}}
\newcommand{\surl}[2]{\,{\tiny \href{#1}{#2}}}


\title[Stokes models with Firedrake]{Stokes glacier models, with Firedrake}

\subtitle{a finite element tutorial}

\author{Ed Bueler}

\institute[UAF]{University of Alaska Fairbanks}

\date[version 2.0]{version 2.0 (July 2026)  \\ \phantom{foo} \\ \phantom{foo} \\
\href{https://github.com/bueler/stokes-ice-tutorial}{\texttt{github.com/bueler/stokes-ice-tutorial}} }


\begin{document}
\beamertemplatenavigationsymbolsempty


\begin{frame}
  \maketitle

\vspace{-14mm}
\hfill \includegraphics[width=0.12\textwidth]{figs/qr.png} \qquad
\end{frame}


\begin{frame}{making this presentation effective}

\begin{itemize}
\item \alert{please ask questions}
\item or send questions or comments, now and in the future:
  \begin{itemize}
  \item[$\circ$] by email to \href{mailto:elbueler@alaska.edu}{\texttt{elbueler@alaska.edu}}
  \item[$\circ$] or by using \href{https://github.com/bueler/stokes-ice-tutorial/issues}{issues at \texttt{github.com/bueler/stokes-ice-tutorial/}}
  \end{itemize}

\bigskip
\item these slides are the primary documentation for some Python codes
\item \alert{please try the codes}, and give me your feedback?  thanks!
\end{itemize}
\end{frame}


\setbeamertemplate{section in toc}[default] 

\newcommand{\outlinelink}{\hspace{20mm} \href{https://github.com/bueler/stokes-ice-tutorial}{\texttt{github.com/bueler/stokes-ice-tutorial}} \par %
%
\vspace{-10mm} %
\hfill \includegraphics[width=0.12\textwidth]{figs/qr.png}}

\begin{frame}
  \frametitle{Outline}

  \tableofcontents[hideallsubsections]

\vspace{5mm}
  \outlinelink
\end{frame}


\section{stage 0 \hspace{9.3mm} equations for the glacier dynamics problem}

\begin{frame}{glacier dynamics using a Stokes model: the high-level view}

\begin{itemize}
\item we apply conservation principles to glaciers:
    \begin{itemize}
    \item[$\circ$] mass conservation \hfill \cmark
    \item[$\circ$] momentum conservation \hfill \cmark
    \item[$\circ$] energy conservation \hfill (not here)
    \end{itemize}
\item incompressibility is one aspect of mass conservation
    \begin{itemize}
    \item[$\circ$] another is the free-surface equation; deferred to \texttt{stage4/}
    \end{itemize}
\item glaciers are a ``slow flow'' problem because inertial forces are tiny
    \begin{itemize}
    \item[$\circ$] momentum conservation is therefore called a ``stress balance''
    \item[$\circ$] velocity \& pressure are \alert{instantaneously determined} by current geometry \& boundary stresses
    \end{itemize}
\item \alert{Stokes equations $=$ stress balance $+$ incompressibility}
\end{itemize}

\bigskip\bigskip

{\scriptsize velocity} \hfill \includegraphics[width=0.8\textwidth]{figs/speed.png}

\medskip
{\scriptsize pressure} \hfill \includegraphics[width=0.8\textwidth]{figs/pressure.png}
\end{frame}


\begin{frame}{Glen-Nye-Stokes equations}

\begin{itemize}
\item the canonical glacier dynamics equations are from Glen, Nye, and Stokes
\item the main equations:
\begin{align*}
- \Div \tau + \nabla p &= \rho \bg &&\text{\emph{stress balance}} &&\phantom{dlfsj fdasj} \\
\Div \bu &= 0 &&\text{\emph{incompressibility}} \\
\tau = B_n &|D\bu|^{(1/n) - 1} D\bu  &&\text{\emph{Glen-Nye flow law}}
\end{align*}

    \begin{itemize}
    \item[$\circ$] notation: $\bu$ is velocity, $p$ is pressure, and $\tau$ is the deviatoric

stress tensor, and $\ds D\bu=\tfrac{1}{2}\left(\grad \bu + \grad \bu^\top\right)$ is the strain-rate tensor
    \end{itemize}

\vspace{-33mm}

%https://www.bl.uk/voices-of-science/interviewees/john-glen#
\hfill \includegraphics[width=0.12\textwidth]{figs/people/jglen.png}
%https://www.igsoc.org/john-f-nye-1923-2019

\hfill {\tiny John Glen}

\medskip
\hfill \includegraphics[width=0.12\textwidth]{figs/people/jnye.jpg}

\hfill {\tiny John Nye}

\vspace{-10mm}

\item constants (\emph{with default values in demos}):
    \begin{itemize}
    \item[$\circ$] $\rho=910 \,\text{kg}\,\text{m}^{-3}$
    \item[$\circ$] $g=|\bg|=9.81\,\text{m}\,\text{s}^{-2}$
    \item[$\circ$] $n=3$
    \item[$\circ$] $B_n=6.8\times 10^7\,\text{Pa}\,\text{s}^{1/3}$ \hfill {\footnotesize $\leftarrow$ \emph{constant because isothermal}}
    \end{itemize}
\end{itemize}
\end{frame}


\begin{frame}{viscosity (and avoiding unbounded viscosity)}

\begin{itemize}
\item the classical viscosity relationship $\tau=2\nu D\bu$ eliminates $\tau$ from the equations, giving a system for $\bu,p$
\item however, the unmodified Glen-Nye viscosity formula $\nu = \frac{1}{2} B_n |D\bu|^{(1/n) - 1}$ yields unbounded values as $D\bu\to 0$, i.e.~for vanishing strain rates
\item so we also regularize with \alert{$\eps>0$} to generate bounded viscosity

\begin{block}{Glen-Nye-Stokes equations with regularized viscosity}

\begin{align*}
- \Div \left(2 \alert{\nu_\eps(|D\bu|)} D\bu\right) + \nabla p &= \rho \bg &&\text{\emph{stress balance}} \\
\phantom{\int} \Div \bu &= 0 &&\text{\emph{incompressibility}} \\
\alert{\nu_\eps(|D\bu|)} &= \tfrac{1}{2} B_n \left(|D\bu|^2 + (\eps\, D_0)^2\right)^{((1/n) - 1)/2} &&\text{\emph{flow law}}
\end{align*}

\smallskip
\end{block}

\item we will use $\eps = 10^{-4}$ and $D_0 = 1\,\text{a}^{-1}$ in the demonstrations
\end{itemize}
\end{frame}


\begin{frame}{boundary conditions, as simple as possible}

\begin{itemize}
\item note that $\sigma = 2 \nu_\eps D\bu - pI$ is the \alert{Cauchy stress tensor}
\item stress-free top:
\begin{equation*}
\sigma \bn = \left(2 \nu_\eps D\bu - pI\right) \bn = \bzero
\end{equation*}

    \begin{itemize}
    \item[$\circ$] atmospheric pressure could be applied at surface \dots no interesting effect
    \end{itemize}
\item no-slip base:
\begin{equation*}
\bu = \bzero
\end{equation*}

    \begin{itemize}
    \item[$\circ$] \alert{no attempt in these slides to model sliding}
    \end{itemize}
\end{itemize}

\bigskip

\begin{center}
\includegraphics[width=0.9\textwidth]{figs/speed.png}

{\tiny speed $|\bu|$}
\end{center}
\end{frame}


\begin{frame}{finite elements \dots with no details}

\begin{itemize}
\item by-hand solution of the Stokes model is impossible for most geometries
    \begin{itemize}
    \item[$\circ$] \emph{exercise.}  solve slab-on-a-slope, which is an exception
    \end{itemize}
\item so we solve numerically using the finite element (FE) method
\item the FE method allow us to start from a mesh of triangles {\small (or quadrilaterals, prisms, \dots) over our domain, {\footnotesize then express the approximate solution in terms of functions built from the mesh, {\scriptsize but I will skip the details \dots}}}

\medskip
\input{figs/trihat.tex}

\vspace{-9mm}
\hfill \includegraphics[width=0.7\textwidth]{figs/stage2.png}

\bigskip
\item the whole point of Firedrake is \alert{suppress the details} of FE
    \begin{itemize}
    \item[$\circ$] but try Elman, Sylvester \& Wathen (2014) for self-study?
    \end{itemize}
\item however, one needs to know a key step:\, \alert{write a weak form}
\end{itemize}
\end{frame}


\begin{frame}{the weak form}

\begin{itemize}
\item start from the equations, also called the \emph{strong form}:
\begin{align}
- \nabla \cdot \left(2 \nu_\eps(|D\bu|)\, D\bu\right) + \nabla p &= \rho \mathbf{g} \\
\Div \bu &= 0
\end{align}
\item multiply (1) by test velocity $\bv$ and (2) by test pressure $q$, integrate by parts, and combine into one nonlinear residual function:
    $$F(\bu,p;\bv,q) = \int_{\Lambda} 2 \nu_\eps(|D\bu|)\, D\bu\,:\,D\bv - p \nabla \cdot \bv - q \nabla \cdot \bu - \rho \mathbf{g} \cdot \bv \,dx$$

    \begin{itemize}
    \item[$\circ$] $\Lambda$ is the domain of ice
    \item[$\circ$] ``$dx$'' $=dx\,dy\,dz$ in 3D, and ``$dx$'' $=dx\,dz$ in the planar case
    \end{itemize}

\medskip
\item the actual weak-form problem statement is ``residual $=0$'':
   $$F(\bu,p;\bv,q) = 0 \qquad \text{ for all test functions } \bv \text{ and } q$$
\item the FE method solves this problem, but over a finite-dimensional space
\end{itemize}
\end{frame}


\begin{frame}[fragile]
\frametitle{UFL for the weak form}

\begin{itemize}
\item the nonlinear residual function:
    $$F(\bu,p;\bv,q) = \int_{\Lambda} 2 \nu_\eps(|D\bu|)\, D\bu\,:\,D\bv - p \nabla \cdot \bv - q \nabla \cdot \bu - \rho \mathbf{g} \cdot \bv \,dx$$
with $\nu_\eps(|D\bu|) = \tfrac{1}{2} B_n \left(|D\bu|^2 + (\eps\, D_0)^2\right)^{((1/n) - 1)/2}$

\smallskip
\item we write this using the \emph{Unified Form Language} (UFL) in Firedrake:

\medskip
\begin{Verbatim}[fontsize=\small]
def D(w):
    return 0.5 * (grad(w) + grad(w).T)

Du2 = 0.5 * inner(D(u), D(u)) + (eps * Dtyp)**2.0
nu = 0.5 * Bn * Du2**((1.0 / n - 1.0)/2.0)

fbody = rho * Constant((0.0, 0.0, - g))
F = ( inner(2.0 * nu * D(u), D(v)) \
      - p * div(v) - q * div(u) - inner(fbody, v) ) * dx
\end{Verbatim}
\end{itemize}
\end{frame}


\begin{frame}[fragile]
\frametitle{mixed finite elements for fluid problems}

\begin{itemize}
\item other details we need to know about:
    \begin{itemize}
    \item[{\color{black} 1.}] choose \alert{separate function spaces} for the velocity and the pressure
    \item[{\color{black} 2.}] choose finite element spaces that (the experts say) are \alert{stable}
    \end{itemize}

\bigskip
\item Firedrake makes choosing such \alert{mixed elements} easy:

\medskip
\begin{Verbatim}[fontsize=\small]
V = VectorFunctionSpace(mesh, 'CG', 2)  # u space is P2
W = FunctionSpace(mesh, 'CG', 1)        # p space is P1
Z = V * W                               # mixed space
up = Function(Z)
u, p = split(up)
v, q = TestFunctions(Z)
\end{Verbatim}

\medskip
    \begin{itemize}
    \item[$\circ$] this $P_2 \times P_1$ choice is called Taylor-Hood
    \item[$\circ$] other mixed spaces are just as easy
    \end{itemize}
\end{itemize}
\end{frame}


\AtBeginSection[]
{
  \begin{frame}{Outline}
  \tableofcontents[currentsection]

\vspace{5mm}
  \outlinelink
  \end{frame}
}

\section{\texttt{stage1/} \qquad first demo: linear Stokes}


\begin{frame}{the linear Stokes equations}

\begin{columns}

\begin{column}{0.6\textwidth}
\begin{itemize}
\item if we make viscosity constant ($\nu_0>0$) then we get a linear Stokes system:
\begin{align*}
- \nabla \cdot \left(2 \nu_0\, D\bu\right) + \nabla p &= \rho \mathbf{g}  \\
\Div \bu &= 0
\end{align*}
\item classical reformat with $2 \nabla \cdot D\bu = \nabla^2 \bu$:
\begin{align*}
- \nu_0 \nabla^2 \bu + \nabla p &= \rho \mathbf{g}  \\
-\Div \bu \qquad \,\,\,\, &= 0
\end{align*}
\item which has symmetric block structure:
  $$\begin{bmatrix} A & B^\top \\ B & 0 \end{bmatrix} \begin{bmatrix} \bu \\ p  \end{bmatrix} = \begin{bmatrix} \bbf \\ 0 \end{bmatrix}$$
with $A\ge 0$, and because $(-\Div)^\top = \grad$ under integration-by-parts
\end{itemize}
\end{column}

\begin{column}{0.4\textwidth}
\hfill \includegraphics[width=0.35\textwidth]{figs/people/gstokes.jpg}

\hfill {\tiny George Stokes}

\vspace{7mm}
\hfill
\begin{tikzpicture}
  \node[anchor=south west, inner sep=0] (img) at (0,0)
    {\includegraphics[width=0.8\textwidth]{figs/Kstokes.pdf}};
  \begin{scope}[x={(img.south east)},y={(img.north west)}]
    % block boundary: pressure DOFs are roughly last 18% of DOFs
    \node[red, font=\Large\bfseries] at (0.38,0.62) {$A$};
    \node[red, font=\Large\bfseries] at (0.91,0.62) {$B^T$};
    \node[red, font=\Large\bfseries] at (0.38,0.09) {$B$};
    \node[red, font=\Large\bfseries] at (0.91,0.09) {$0$};
  \end{scope}
\end{tikzpicture}
\end{column}

\end{columns}
\end{frame}


\begin{frame}[fragile]
\frametitle{running the demonstration programs}

\begin{itemize}
\item let's run a demo

\medskip
\item install Firedrake following these directions:

\begin{center}
\href{https://www.firedrakeproject.org/install.html}{\texttt{firedrakeproject.org/install.html}}
\end{center}
\item activate the Python virtual environment every time you use Firedrake:

\medskip
\begin{Verbatim}[fontsize=\small]
$ source ~/venv-firedrake/bin/activate
\end{Verbatim}
%$

\medskip
\item other tools you will need:
    \begin{itemize}
    \item[$\circ$] Gmsh \quad \href{https://gmsh.info/}{\texttt{gmsh.info}}
    \item[$\circ$] Paraview \quad \href{https://www.paraview.org/}{\texttt{paraview.org}}
    \end{itemize}

\medskip
\item clone this tutorial, both for the codes and these slides:

\bigskip
\begin{Verbatim}[fontsize=\small]
$ git clone https://github.com/bueler/stokes-ice-tutorial.git
\end{Verbatim}
%$

\vspace{-22mm}

\hfill \includegraphics[width=0.12\textwidth]{figs/qr.png} \qquad

\vspace{5mm}
\end{itemize}
\end{frame}


\begin{frame}[fragile]
\frametitle{\texttt{stage1/} \qquad linear Stokes}

\begin{itemize}
\item \emph{purpose:} solve linear Stokes on a trapezoidal ``glacier''

    \begin{itemize}
    \item[$\circ$] weak form:
    \end{itemize}
{\small
    $$F(\bu,p;\bv,q) = \int_{\Lambda} 2 \nu_0\, D\bu\,:\,D\bv - p \nabla \cdot \bv - q \nabla \cdot \bu - \rho \mathbf{g} \cdot \bv \,dx$$
}
\item \emph{source files:} \texttt{domain.geo}, \texttt{solve.py} \hfill \alert{$\gets$ inspect these}
\item \emph{generated files:} \texttt{domain.msh}, \texttt{domain.pvd}
\end{itemize}

\bigskip
\begin{Verbatim}
$ cd stage1/
$ gmsh -2 domain.geo             # mesh the domain
$ gmsh domain.msh                # view the mesh
$ python3 solve.py               # solve linear Stokes
$ paraview domain.pvd            # view the solution
\end{Verbatim}
%$

\vspace{5mm}
\begin{center}
\includegraphics[width=0.9\textwidth]{figs/stage1.png}

{\tiny speed $|\bu|$}
\end{center}
\end{frame}


\section{\texttt{stage2/} \qquad Glen-Nye-Stokes for glacier ice}

\begin{frame}{Newton's method}

\begin{columns}
\begin{column}{0.8\textwidth}
\begin{itemize}
\item linear Stokes \small (\texttt{stage1/}) \normalsize solves a single linear system:
  $$\begin{bmatrix} A & B^\top \\ B & 0 \end{bmatrix} \begin{bmatrix} \bu \\ p  \end{bmatrix} = \begin{bmatrix} \bbf \\ 0 \end{bmatrix}$$

    \begin{itemize}
    \item[$\circ$] default solver: Firedrake $\to$ PETSc KSP $\to$ MUMPS
    \end{itemize}
\item but Glen-Nye-Stokes residual function is \alert{nonlinear in $\bu$}:
\small
$$F(\bu,p;\bv,q) = \int_{\Lambda} 2\, \alert{\nu_\eps(|D\bu|)\, D\bu}\,:\,D\bv - p \nabla \cdot \bv - q \nabla \cdot \bu - \rho \mathbf{g} \cdot \bv \,dx$$
\normalsize
\item fortunately, Firedrake applies Newton's method by default if we write the problem as\, \texttt{solve(F == 0,\dots)}
\item what you need to know about Newton's iteration:
    \begin{itemize}
    \item[$\circ$] it does linearization around each iterate
    \item[$\circ$] Firedrake uses UFL symbolic differentiation for linearization
    \item[$\circ$] Firedrake asks PETSc's SNES to do the Newton iteration
    \item[$\circ$] SNES options will monitor and control the iteration
    \end{itemize}
\end{itemize}
\end{column}
\begin{column}{0.2\textwidth}
\vspace{25mm}

\hfill \includegraphics[width=0.8\textwidth]{figs/people/inewton.jpg}

\hfill {\tiny Isaac Newton}
\end{column}
\end{columns}
\end{frame}


\begin{frame}[fragile]
\frametitle{\texttt{stage2/} \qquad Glen-Stokes}

\begin{itemize}
\item \emph{purpose:} solve Glen-Stokes on a flat-bed glacier
\item \emph{source files:} \texttt{domain.py}, \texttt{solve.py} \hfill \alert{$\gets$ inspect these}
\item \emph{generated files:} \texttt{dome.geo}, \texttt{dome.msh}, \texttt{dome.pvd}
\end{itemize}

\bigskip
\begin{Verbatim}[fontsize=\small]
  $ cd stage2/
  $ python3 domain.py       # generate the domain outline
  $ gmsh -2 dome.geo        # mesh the domain
  $ python3 solve.py        # solve Glen-Nye-Stokes
  $ paraview dome.pvd       # view the solution
\end{Verbatim}
%$

\vspace{6mm}
\begin{center}
\includegraphics[width=0.95\textwidth]{figs/stage2.png}

{\tiny speed $|\bu|$}
\end{center}
\end{frame}


\section{\texttt{stage3/} \qquad more power: extruded meshes and 3D glaciers}

\begin{frame}[fragile]
\frametitle{a more practical and powerful solver}

\begin{itemize}
\item the goal of this stage is to advance toward a practical and powerful glacier-modeling tool
\item we switch to an \alert{extruded mesh}, avoiding Gmsh in the toolchain
    \begin{itemize}
    \item[$\circ$] also we allow a bumpy bed (below)
    \item[$\circ$] now the elements are quadrilaterals ($Q_2\times Q_1$ mixed method) in the planar case, versus triangles in \texttt{stage2/}
    \end{itemize}
\item we demonstrate (mostly) \alert{dimension-independent programming}
    \begin{itemize}
    \item[$\circ$] code for 2D and 3D glacier geometry is nearly the same
    \end{itemize}
\item additional bells \& whistles:
    \begin{itemize}
    \item[$\circ$] rescaling the equations
    \item[$\circ$] vertical grid sequencing, with over-regularization on coarse meshes
    \item[$\circ$] additional diagnostic fields in the output \texttt{.pvd} file
    \end{itemize}
\end{itemize}

\bigskip

\noindent {\footnotesize solution speed $|\bu|$ for default run:}

\medskip
\includegraphics[width=\textwidth]{figs/stage3speed.png}
\end{frame}


\begin{frame}[fragile]
\frametitle{\texttt{stage3/} \qquad extruded mesh and more}

\begin{itemize}
\item \emph{purpose:} a practical and robust solver
    \begin{itemize}
    \item[$\circ$] construct mesh by vertically extruding a base mesh  \hfill \emph{refinement framework}
    \item[$\circ$] rescale the equations  \hfill \emph{better conditioning}
    \item[$\circ$] vertical grid sequencing  \hfill \emph{better initial iterates}
    \item[$\circ$] write $\nu_\eps$ and $\tau$ from solution  \hfill \emph{diagnostic}
    \end{itemize}
\item \emph{source file:} \texttt{solve.py} \hfill \alert{$\gets$ inspect this}
\item \emph{generated file:} \texttt{dome.pvd}
\end{itemize}

\smallskip
\begin{Verbatim}[fontsize=\small]
$ cd stage3/
$ python3 solve.py                    # default resolution
$ python3 solve.py -mx 400 -refine 2  # higher resolution
$ paraview dome.pvd
\end{Verbatim}
%$

\bigskip

\noindent {\footnotesize effective viscosity $\nu_\eps$ and deviatoric stress magnitude $|\tau|$:}

\medskip
\begin{center}
\includegraphics[width=0.8\textwidth]{figs/stage3nu.png}

\smallskip
\includegraphics[width=0.8\textwidth]{figs/stage3tau.png}
\end{center}
\end{frame}


\begin{frame}[fragile]
\frametitle{evidence of convergence}

\begin{itemize}
\item our \texttt{stage3/} code sets resolution at runtime
\item for example:
\begin{Verbatim}
$ python3 solve.py -b0 0 -mx 400 -mz 4 -refine 2
\end{Verbatim}
%$
    \begin{itemize}
    \item[$\circ$] using flat bed (\texttt{-b0 0}); compare results to \texttt{stage2/}
    \item[$\circ$] initial mesh has 400 elements in horizontal and 4 in vertical
    \item[$\circ$] refine the vertical twice (by factors of four), to generate a $400\times 64$ mesh
    \end{itemize}
\item velocity results, in meters per year, from a refinement study:

\medskip
% -b0 0 -mx 50 -mz 2 -refine 1     50 x 8      400m x 125m
% -b0 0 -mx 100 -mz 4 -refine 1    100 x 16    200m x 63m
% -b0 0 -mx 200 -mz 2 -refine 2    200 x 32    100m x 31m
% -b0 0 -mx 400 -mz 4 -refine 2    400 x 64    50m x 16m
% -b0 0 -mx 800 -mz 2 -refine 3    800 x 128   25m x 8m
\begin{center}
\small
\begin{tabular}{c|rrr}
mesh           &   $\Delta x\times \Delta z$ (m) & average $|\bu|$ & maximum $|\bu|$ \\ \hline
$50\times 8$   &   $400 \times 125$ &        1782 &     3263     \\
$100\times 16$ &   $200 \times 63$  &        1766 &     3211     \\
$200\times 32$ &   $100 \times 31$  &        1761 &     3196     \\
$400\times 64$ &   $50  \times 16$  &        1759 &     3191     \\
$800\times 128$&   $25  \times 8$   &        1759 &     3190    
\end{tabular}
\normalsize
\end{center}

    \begin{itemize}
    \item[$\circ$] velocities are apparently converging
    \item[$\circ$] however, we do not know the exact solution \dots this is not verification
    \end{itemize}
\end{itemize}
\end{frame}


\begin{frame}[fragile]
\frametitle{parallel computation}

\begin{itemize}
\item as usual with Firedrake, everything works in parallel
    \begin{itemize}
    \item[$\circ$] extruded mesh gets partitioned following base mesh partition
    \item[$\circ$] see \texttt{rank} field in \texttt{.pvd} output, for example with 4 processes:

\smallskip
\qquad \includegraphics[width=0.85\textwidth]{figs/stage3rank.png}

\medskip
    \item[$\circ$] using the default direct solver (Mumps), solution values are essentially independent of number of processes
    \end{itemize}
\item however, speed-up is sublinear because of significant serial workloads

\medskip
\begin{Verbatim}[fontsize=\footnotesize,xleftmargin=-4mm]
$ python3 solve.py -mx 800 -refine 2               # 38 secs run time

$ mpiexec -n 4 python3 solve.py -mx 800 -refine 2  # 20 secs run time
\end{Verbatim}
%$

\medskip
\item your performance results may vary \dots
\end{itemize}
\end{frame}


\begin{frame}[fragile]
\frametitle{\texttt{stage3/} continued \qquad a 3D glacier}

\begin{itemize}
\item \emph{purpose:} demonstrate a (mostly) dimension-independent Firedrake code which permits a 3D glacier
\item \emph{source file:} \texttt{solve.py} \hfill \alert{$\gets$ inspect this}
\item \emph{generated file:} \texttt{dome.pvd}
\end{itemize}

\medskip
\begin{Verbatim}
$ python3 solve.py -dim 3 -baserefine 3 -refine 1
$ paraview dome.pvd
\end{Verbatim}

\begin{center}
\includegraphics[width=0.6\textwidth]{figs/stage3speed3d.png}

\bigskip
\scriptsize 3$\times$ vertical exaggeration, coloring by speed $|\bu|$
\end{center}
\end{frame}


\begin{frame}[fragile]
\frametitle{solver performance limitations}

\begin{itemize}
\item this run is about as far as I can refine on my office desktop:

\begin{Verbatim}[fontsize=\small]
$ mpiexec -n 12 python3 solve.py -dim 3 -baserefine 4 \
    -refine 2 -s_snes_atol 1.0e-2 -o finest.pvd
\end{Verbatim}
%$

    \begin{itemize}
    \item[$\circ$] 100 GB ram, plenty of cores, half hour run time
    \end{itemize}

\bigskip
\item \emph{limiting factor:} the \alert{direct solver} for each Newton step linear system uses too much memory when generating the LU factors
    \begin{itemize}
    \item[$\circ$] direct solvers experience \emph{fill-in}, especially in 3D, and they are slow
    \item[$\circ$] see B (2021b, 2023)
    \end{itemize}
\item for higher resolution we need a scalable solver
\item solver composition: matrix-free, Schur-complement preconditioning, multigrid on the $\bu$-$\bu$ block
    \begin{itemize}
    \item[$\circ$] see Isaac, Stadler, \& Ghattas (2015) for such a scalable solver
    \item[$\circ$] addressing this properly is a different talk!
    \end{itemize}
\end{itemize}
\end{frame}


\section{\texttt{stage4/} \qquad evolving glaciers: coupling the free-surface equation}

\begin{frame}{but don't glaciers actually change with the climate?}

\begin{itemize}
\item we do not yet have a ``real'' glacier model
    \begin{itemize}
    \item[$\circ$] the climate (e.g.~surface mass balance) has not appeared as an input
    \item[$\circ$] time derivatives have not appeared either!
    \end{itemize}

\medskip
\item[Q.] how do glaciers change shape?

\item[A.] via the \alert{free-surface equation} (surface kinematical equation):
   $$\frac{\partial s}{\partial t} - \bu|_s \cdot \bn_s = a$$

    \begin{itemize}
    \item[$\circ$] $z=s(t,x,y)$ is the surface elevation of the glacier

\smallskip
    \item[$\circ$] $a(t,x,y)$ is the surface mass balance \strut
    \item[$\circ$] $\bn_s=\left<-\frac{\partial s}{\partial x},-\frac{\partial s}{\partial y},1\right>$ is a normal vector to the surface

\smallskip
    \item[$\circ$] $\bu|_s$ is the surface value (\emph{trace}) of the Stokes solution velocity
    \end{itemize}

\medskip
\item the free-surface equation holds on $[0,T] \times \Omega$
    \begin{itemize}
    \item[$\circ$] $\Omega \subset \RR^d$ is a fixed map-plane region \hfill {\footnotesize \emph{$d=2$ in reality, $d=1$ for planar}}
    \end{itemize}
\end{itemize}
\end{frame}


\begin{frame}{coupled system of equations}

\begin{itemize}
\item so we have a \alert{coupled} system of simultaneous equations

\begin{block}{coupled free-surface \& Stokes equations}
\begin{align*}
\frac{\partial s}{\partial t} - \bu|_s \cdot \bn_s &= a &&\text{\emph{free-surface equation}} \\
- \Div \left(2 \nu_\eps(|D\bu|)\, D\bu\right) + \nabla p &= \rho \bg &&\text{\emph{stress balance}} \strut \\
\Div \bu &= 0 &&\text{\emph{incompressibility}} \phantom{\int}
\end{align*}
\end{block}
\end{itemize}

\medskip
\begin{columns}
\begin{column}{0.5\textwidth}
\begin{itemize}
\item the coupling has two aspects:
\end{itemize}
\begin{enumerate}
\item[I.] the free-surface equation has $\bu|_s$ as a coefficient
\item[II.] $s$ determines the \alert{evolving domain $\Lambda(t)$} on which the Stokes problem is solved
\end{enumerate}
\end{column}
\begin{column}{0.5\textwidth}
\mbox{\, \includegraphics[width=0.95\textwidth]{figs/domain.png}}
\end{column}
\end{columns}
\end{frame}


\begin{frame}{weak form of coupled system of equations}

\begin{itemize}
\item get the weak form of the coupled system in the usual way \dots
\item multiply by test functions and integrate by parts:
{\small
\begin{align*}
\int_\Omega \left(\frac{\partial s}{\partial t} - \bu|_s \cdot \bn_s - a\right)\, r\,dx &=0  &&\forall r \\
\int_{\Lambda(t)} 2\, \nu_\eps(|D\bu|)\, D\bu\,:\,D\bv - p \nabla \cdot \bv - \rho \mathbf{g} \cdot \bv\,dx &= 0 &&\forall \bv \\
\int_{\Lambda(t)} - (\Div \bu) q\,dx &= 0 &&\forall q
\end{align*}
}
\end{itemize}

\begin{columns}
\begin{column}{0.55\textwidth}
\begin{itemize}
\item we need to make high-level choices about this coupled system:
    \begin{enumerate}
    \item[1.] time-stepping: explicit or implicit?
    \item[2.] solved together (\emph{monolithic}) or split?
    \item[3.] spaces and elements for $s,\bu,p$?
    \end{enumerate}
\end{itemize}
\end{column}
\begin{column}{0.45\textwidth}
\phantom{foo}

%\vspace{2mm}
\hfill \includegraphics[width=\textwidth]{figs/domain.png}
\end{column}
\end{columns}
\end{frame}


\begin{frame}{challenges in solving the coupled system}

\begin{itemize}
\item early/naive approaches to solving this coupled problem have been fragile
    \begin{itemize}
    \item[$\circ$] The method everyone tries first is to solve Stokes for the current geometry, then advance the surface, and repeat.
    \item[$\circ$] This is a \emph{split} and \emph{explicit} approach to the coupled problem.
    \item[$\circ$] The result is \alert{limited and uncertain performance}.
    \end{itemize}

\bigskip
\item<2> what are the underlying challenges in designing better schemes?
\end{itemize}

\begin{enumerate}
\item<2> The surface elevation $s$ determines the evolving domain $\Lambda(t)$ on which the Stokes problem is solved.  The mesh used for the $\bu,p$ solver therefore changes at every time step.  This makes monolithic and/or implicit implementations difficult.
\item<2> The coupled problem mixes advective and diffusive aspects.  The proportions depend on the particular boundary conditions.
    \begin{itemize}
    \item Precise time-step restriction and expected performance scaling remain unclear (B 2023 and references therein).
    \end{itemize}
\end{enumerate}
\end{frame}


\begin{frame}{choices for \texttt{stage4/}}

\begin{itemize}
\item we will demonstrate and test a solver which is \alert{semi-implicit}, \alert{split}, \alert{stabilized}, and \alert{free margin}
    \begin{itemize}
    \item[$\circ$] warning: ``stabilized'' means several things here
    \item[$\circ$] we use $Q_1$ elements for $s$ in the planar case; thus \alert{$\Lambda(t)$ is polygonal}
    \item[$\circ$] as before, we use an extruded mesh with inf-sup stable $Q_2\times Q_1$ elements for $\bu,p$
    \end{itemize}

\smallskip
\item our solver is based on these non-trivial choices:

\smallskip
    \begin{enumerate}
    \item[1.] first-order, and mostly explicit, time-stepping
        \begin{itemize}
        \item[A.] but we add an implicit \alert{edge stabilization} in the free-surface equation
        \item[B.] and we determine the time step by a CFL condition
        \end{itemize}

\smallskip
    \item[2.] the coupled system is split, with alternating free-surface and Stokes solves
        \begin{itemize}
        \item[$\circ$] but we add a \alert{load stabilization} coupling to the Stokes solver
        \end{itemize}

\smallskip
    \item[3.] the coupled free-surface problem is solved as a \alert{variational inequality} which determines the glacier extent and margin
        \begin{itemize}
        \item[A.] we apply a bounds-respecting Newton method
        \item[B.] we allow variable ice extent on the extruded mesh
        \end{itemize}
    \end{enumerate}

\medskip
\item the next slides give citations and details on techniques 1, 2, 3
\end{itemize}
\end{frame}


\newcommand{\jump}[1]{\llbracket #1 \rrbracket}


\begin{frame}{technique 1A: implicit edge stabilization in free-surface equation}

\begin{itemize}
\item this technique is from Tominec et al (2026)
    \begin{itemize}
    \item[$\circ$] from Burman \& Hansbro (2004), Ern \& Guermond (2013)
    \end{itemize}
\item add FE \alert{edge stabilization}, applied \alert{implicitly}:
    $$\int_{\Omega^h} \left(\frac{\alert{s_{k+1}^h} - s_k^h}{\Delta t} - \bu|_{s_k^h} \cdot \bn_{s_k^h} - a\right) r^h\,dx + J(\alert{s_{k+1}^h},r^h) = 0$$
for all test functions $r^h$, where
    $$J(s,r) = \sum_{e\in E^h} \int_e \tfrac{1}{2} h_e^2 \left|\bu|_{s_k^h}\right| \jump{\grad s} \jump{\grad r}\,dS$$

    \begin{itemize}
    \item[$\circ$] $s_k^h$ is the FE approximation of surface elevation at time $t_k$
    \item[$\circ$] $E^h$ is the set of interior edges in the triangulation $\Omega^h$
    \item[$\circ$] $\jump{\bv} = \bv_+ \cdot \bn_+ - \bv_- \cdot \bn_-$ is the jump of a vector field along an edge
    \item[$\circ$] $h_e$ is the average of the diameters of the two cells which meet $e$
    \item[$\circ$] note surface velocity and slope are evaluated explicitly, at $t_k$
    \item[$\circ$] the system to solve for $\alert{s_{k+1}^h}$ is \emph{linear}
    \end{itemize}
\end{itemize}
\end{frame}


\begin{frame}{technique 1B: CFL determination of time step}

\begin{itemize}
\item the edge stabilization technique applies semi-implicitly only, with \alert{velocity evaluated explicitly}

\item our application has an advancing glacier margin, which is not considered in the proof of stability in Tominec et al (2026)

\medskip
\item it appears the time step $\Delta t$ must still be limited by a CFL\footnote{$=$ Courant-Friedrichs-Lewy} criterion:
    $$\alert{\Delta t = C_{\text{CFL}} \frac{\min h}{\max \big|\bu\big|}}$$

    \begin{itemize}
    \item[$\circ$] thus ice motion per step ($|\bu| \Delta t$) is at most base-mesh element diameter ($h$)
    \item[$\circ$] we get reasonable results with $C_{\text{CFL}}=0.25$ \strut
        \begin{itemize}
        \item Ern \& Guermond (2013) use $C_{\text{CFL}}=0.2$ or $0.25$ with weighted edge stabilization and SSP3, RK4 schemes
        %\item contrast the steady-state analysis in Burman \& Hansbro (2004)
        \end{itemize}
    \end{itemize}
\end{itemize}
\end{frame}


\begin{frame}{technique 2: semi-implicit coupling heuristic in stress balance}

\begin{itemize}
\item also from Tominec et al (2026), but based on L\"ofgren et al (2022)
\item add \alert{extrapolated surface mass balance load} to the Stokes form:
{\small
\begin{align*}
\int_{\Lambda(t_k)} 2\, \nu_\eps(|D\bu|)\, D\bu\,:\,D\bv - p \nabla \cdot \bv - \rho \mathbf{g} \cdot \bv\,dx \qquad& \\
\alert{+ \int_{\Gamma_s(t_k)} \rho g \Delta t \left[\frac{1}{2} (\bu\cdot \bn_{s_k}) + a\right] (\bv\cdot \bn) \,ds} &= 0
\end{align*}

    \begin{itemize}
    \item[$\circ$] $\Lambda(t_k)$ is the Stokes domain defined by the current surface elevation $s_k$
    \item[$\circ$] $\Gamma_s(t_k)$ is the upper surface of $\Lambda(t_k)$
    \item[$\circ$] $\bn_{s_k}=\left<-\grad s_k,1\right>$ is an outward normal on $\Gamma_s(t_k)$
    \item[$\circ$] $\bn=\bn_{s_k}/|\bn_{s_k}|$ is the outward \emph{unit} normal 
    \end{itemize}
}

\medskip
\item concept: a modified stress balance stabilizes the coupling
    \begin{itemize}
    \item[$\circ$] L\"ofgren et al (2022) use Reynold's transport theorem to motivate this
    \item[$\circ$] \dots but the above form is symmetrized relative to their version
    \item[$\circ$] Tominec et al (2026) \alert{prove} that techniques 1 \& 2 together stabilize the $L^2$ norm of the evolving surface type of stabilization of the coupled system
    \end{itemize}
\end{itemize}
\end{frame}


\begin{frame}[fragile]
\frametitle{\texttt{stage4/} \qquad slumping in a box}

\begin{itemize}
\item \emph{purpose:} evolving surface between lubricated walls, exhibiting \alert{numerical stability} and \alert{exact mass conservation}
    \begin{itemize}
    \item[$\circ$] these boundary conditions are considered by L\"ofgren et al (2022) and Tominec et al (2026)
    \end{itemize}
\item \emph{source files:} \texttt{geometry.py}, \texttt{solve.py} \hfill \alert{$\gets$ inspect these}
\item \emph{generated file:} \texttt{dome.pvd}
\end{itemize}

\bigskip
\begin{Verbatim}
$ cd stage4/
$ python3 solve.py -walls -L 8000
$ paraview dome.pvd
\end{Verbatim}
%$


\bigskip

{\footnotesize $t=0$ a:}\, \includegraphics[width=0.8\textwidth]{figs/stage4wallsstart.png}

\centerline{$\to$}

\medskip
\hfill {\footnotesize $t=5$ a:}\, \includegraphics[width=0.8\textwidth]{figs/stage4wallsend.png}
\end{frame}


\begin{frame}[fragile]
\frametitle{instability showing need for stabilization technique 2}

\begin{itemize}
\item without technique 2, the maximum size of the stable time step is significantly restricted
    \begin{itemize}
    \item[$\circ$] runs produce ``sloshing'' (L\"ofgren et al 2022) and Stokes solver failures
    \end{itemize}
\item for each resolution in this table we start with $\Delta t=1$ a, and reduce $\Delta t$ by a factor of two until the $5$ a run completes without a solver failure

\medskip
\begin{center}
\small
\begin{tabular}{c|c}
$\Delta x$ (m) & stable $\Delta t$ (a) w/o technique 2 \\ \hline
$640$ & $0.25$ \\
$320$ & $0.25$ \\
$160$ & $0.125$ \\
$80$ & $0.125$ \\
$40$ & $0.125$
\end{tabular}
\normalsize
\end{center}

\smallskip
    \begin{itemize}
    \item[$\circ$] to produce table:  \verb|$ python3 solve.py -walls -L 8000| \verb|                -noload -nocfl -mx MX -maxdt DT|

with \texttt{MX}$=25,50,100,200,400$
    \item[$\circ$] Tominec et al (2026) does more experimentation
    \end{itemize}
\end{itemize}
\end{frame}


\begin{frame}{but glaciers have moving margins!}

\begin{itemize}
\item however, the \alert{surface elevation must be above the bed}
\item enforcing this obvious fact provides the (lateral) free-boundary condition for a land-based glacier
    \begin{itemize}
    \item[$\circ$] a \alert{variational inequality} (B 2021b, Chapter 12) enforces it \dots next slide
    \item[$\circ$] the thickness and flux are both zero at the free boundary
    \end{itemize}
\item solving the coupled problem subject to this condition determines the location of the moving \alert{glacier margin}
    \begin{itemize}
    \item[$\circ$] ``free boundary'' and ``glacier margin'' are synonyms here
    \end{itemize}
\end{itemize}

\bigskip
\centering
\includegraphics[width=0.55\textwidth]{figs/domain-margin.png}
\end{frame}


\begin{frame}{technique 3A: variational inequality for free surface}

\begin{itemize}
\item suppose a (time-independent) bed elevation function $b(x,y)$
    \begin{itemize}
    \item[$\circ$] the meshed $\text{CG}_1$ bed is denoted $b^h$
    \end{itemize}
\item define the FE \alert{admissible set} $\cK^h = \left\{r^h \in \text{CG}_1 \,:\, r^h\ge b^h\right\}$
\item updated surface \alert{$s_{k+1}^h \in \cK^h$} solves this variational inequality weak form:
    $$\int_\Omega \left(\frac{s_{k+1}^h - s_k^h}{\Delta t} - \bu|_{s_k^h} \cdot \bn_{s_k^h} - a\right)\, (r^h-s_{k+1}^h)\,dx + J(s_{k+1}^h,r^h-s_{k+1}^h) \ge 0$$
for all $r^h$ in $\cK^h$

    \begin{itemize}
    \item[$\circ$] includes edge stabilization
    \end{itemize}

\item the computed margin location

is where $s_{k+1}^h$ meets $b^h$

\item we apply the \texttt{vinewtonrsls}

bounds-respecting Newton

solver from PETSc (B 2021b)

\vspace{-34mm}
\hfill \includegraphics[width=0.5\textwidth]{figs/domain-margin.png}
\end{itemize}
\end{frame}


\begin{frame}{technique 3B: extruded mesh with variable ice extent}

\begin{itemize}
\item at each time step the current surface $s^h_k$ defines the ice domain $\Lambda(t_k)$

\smallskip
\item on the other hand, our extruded mesh covers the entire map-plane region $\Omega$ with a fixed number \texttt{mz} of levels in the vertical
    \begin{itemize}
    \item[$\circ$] the \alert{base mesh} covers $\Omega$
    \item[$\circ$] $\bu,p$ are defined at each node of the extruded mesh
    \item[$\circ$] all of our meshes and fields have \alert{constant memory footprint} as we time-step
    \end{itemize}

\medskip
\item if $s^h_k=b^h$ for some base mesh node then there is no ice in that column of the extruded mesh, and so \dots
\item \alert{in extruded mesh columns with no ice we lock $\bu,p$ values to zero}
    \begin{itemize}
    \item[$\circ$] implemented in classes \texttt{PinchColumnPressure} and \texttt{PinchColumnVelocity} in \texttt{stage4/geometry.py}
    \item[$\circ$] \texttt{PinchColumnX} are sub-classes of Firedrake's \texttt{DirichletBC}
    \end{itemize}
\end{itemize}
\end{frame}


\begin{frame}[fragile]
\frametitle{\texttt{stage4/} continued \qquad a dome flowing outward}

\begin{itemize}
\item \emph{purpose:} evolving free surface with a moving margin, exhibiting numerical stability
\item \emph{source files:} \texttt{geometry.py}, \texttt{solve.py} \hfill \alert{$\gets$ inspect these}
\item \emph{generated file:} \texttt{movie.pvd}
\end{itemize}

\medskip
\begin{Verbatim}[fontsize=\small,xleftmargin=5mm]
$ python3 solve.py -mx 200 -mz 10 -T 100 -L 20000 \
    -omovie movie.pvd
$ paraview movie.pvd
\end{Verbatim}

\bigskip

\mbox{\hspace{-12mm} \includegraphics[width=0.51\textwidth]{figs/stage4movingstart.png} $\to$ \quad \includegraphics[width=0.51\textwidth]{figs/stage4movingend.png}}

\mbox{\hspace{15mm} \footnotesize $t=0$ a} \hspace{59mm} {\footnotesize $t=100$ a}

\centerline{\footnotesize 10$\times$ vertical exaggeration, $\log(|\bu|)$ coloring}
\end{frame}


\begin{frame}[fragile]
\frametitle{instability showing need for stabilization technique 1}

\begin{itemize}
\item especially near the glacier margin, edge stabilization (technique 1A) is clearly needed
    \begin{itemize}
    \item[$\circ$] runs with \texttt{-walls} do not show the need
    \end{itemize}
\item compare these runs:
\begin{Verbatim}[fontsize=\small,xleftmargin=5mm]
$ python3 solve.py -mx 200 -T 1
\end{Verbatim}
%$

\hfill \includegraphics[width=0.7\textwidth]{figs/marginusual.png}

\begin{Verbatim}[fontsize=\small,xleftmargin=5mm]
$ python3 solve.py -mx 200 -T 1 -noedge
\end{Verbatim}
%$

\hfill \includegraphics[width=0.7\textwidth]{figs/marginnoedge.png}

\medskip
\item the need to use CFL for time-step length (technique 1B) is more subtle
    \begin{itemize}
    \item[$\circ$] compare the margin shape at the end of these two runs:

\begin{Verbatim}[fontsize=\small,xleftmargin=10mm]
$ python3 solve.py -mx 100
$ python3 solve.py -mx 100 -nocfl -maxdt 1.0
\end{Verbatim}
    \item[$\circ$] the first run, using CFL, had time steps varying from $0.02$ a to $0.25$ a
    \item[$\circ$] the margin shape in the second run is clearly wrong, but not wildly unstable either (\emph{not shown})
    \end{itemize}
\end{itemize}
\end{frame}


\begin{frame}{mass conservation errors with free boundaries}

FIXME mass conservation approximate; needs resolution

FIXME issue in B (2021a) is different; here we should conserve
\end{frame}


\begin{frame}{evidence of correctness}

FIXME compare Halfar
\end{frame}


\begin{frame}{learn more}

\qquad \large \href{https://github.com/bueler/stokes-ice-tutorial}{\texttt{github.com/bueler/stokes-ice-tutorial}}

\vspace{-5mm}
\hfill \includegraphics[width=0.1\textwidth]{figs/qr.png}

\begin{columns}
\begin{column}{0.6\textwidth}
\small

\begin{itemize}
\item Firedrake: \href{https://www.firedrakeproject.org/}{\texttt{firedrakeproject.org}}
\item PETSc: \href{https://petsc.org}{\texttt{petsc.org}}

\vspace{15mm}
\item for Glen-Nye-Stokes eqns, see Ch.~1 by Hewitt:  Fowler \& Ng (2021)

\item for finite elements and Stokes:  Elman, Silvester, \& Wathen (2014)

\item for PETSc, Firedrake, and Stokes:  Bueler (2021b)
\end{itemize}
\end{column}

\begin{column}{0.4\textwidth}

\bigskip
\href{https://www.firedrakeproject.org/}{\includegraphics[width=\textwidth]{figs/firedrakebanner.png}}

\bigskip
\hfill \href{https://www.mcs.anl.gov/petsc/}{\includegraphics[width=0.6\textwidth]{figs/petscbanner.png}}

\vspace{10mm}
\includegraphics[width=0.31\textwidth]{figs/covers/karthaus.png} \hfill
\includegraphics[width=0.31\textwidth]{figs/covers/elman.jpg} \hfill
\href{https://my.siam.org/Store/Product/viewproduct/?ProductId=32850137}{\includegraphics[width=0.32\textwidth]{figs/covers/bueler.jpg}}
\end{column}

\end{columns}
\end{frame}


\begin{frame}{references}

{\scriptsize \input{references.tex}}
\end{frame}


\end{document}
